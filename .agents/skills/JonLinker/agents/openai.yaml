interface:
  display_name: "JonLinker"
  short_description: "Repo-specific patterns and workflows for JonLinker"
  default_prompt: "Use the JonLinker repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true